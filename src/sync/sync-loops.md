# `sync/` — the reconciliation loops

The background engine that keeps mirrors up to date in both directions. Runs on timers;
also driven on demand by nudges (`../p2p/protocol.ts`) so changes land in seconds.

| File                   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `invites.ts`           | **Native album invitations, per person.** Every person on a linked server gets a local marker user; adding one to an album in Immich's own picker shares that album with that person, and removing them revokes it. The origin detects this by listing albums as each marker; members discover it by polling the `/invitations` peer route (over iroh). A successful poll retires missing `via: 'invite'` mirrors even if their mapping is dead. A share appearing or being withdrawn nudges the other household (`nudgePeerInvitations` → `/invitations/nudge`) so their mirror follows in about a second rather than at their next sweep. Server _linking_ is not here — it is admin-owned, in [`p2p/unlink.ts`](../p2p/) and the panel.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `engine.ts`            | Photo sync. `watchOnce` pushes local additions out to peers; member-mirror album reads use the `Mapping.hostSlug` utility key because the configured admin may not have `album.read` access. `reconcileOnce`/`reconcileMapping` pull the origin manifest, materialise anything missing, and **propagate deletions** (with a consistency gate so an indexing lag never wrongly deletes). `pushAlbumRefs` is that push on its own, so a reunion offers the album it just adopted without waiting for the watcher's next cycle. `startWatchLoop` runs it on an overlap-guarded interval, and every loop here is held while `sweeps.ts` says so (rig-only: `/test/pause-sweeps`) — a change that still arrives with them held was pushed, not swept.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `leave.ts`             | `leaveAlbum` — the full reverse of a join: purges every stub, the mirror album, the mapping and its ledger. An asset another mapping's authoritative row (`ledgerByAsset`) still claims is retained — a bookkeeping row cannot pin one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `matches.ts`           | **Reunification matching, pure.** `albumsIPublish` turns Immich's album list into the index a person may offer (only albums they OWN — Immich says so inside `albumUsers`, and a response carries no `ownerId`); `matchAlbums` pairs same-named albums owned by different people, ordering by date overlap and count but never filtering on them, and answers **one row per pairing it can tell apart** — a name the peer holds twice under the same owner, count and dates is one row, because the panel would print it identically and every action is resolved by name (`asOneRow`). No Immich call and no storage, so it is unit-tested in milliseconds. See [`../docs/post-v1-reunification-design.md`](../docs/post-v1-reunification-design.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `album-index.ts`       | **The album index itself.** `publishOwnedAlbums` reads the caller's OWN albums from Immich on their forwarded credential and records them for one peer under a `direction`: `to-them` is what we offer, served by the `/albums` peer route, and `from-them` is what that peer's route answered, which matching compares against. The two directions are separate rows — `publishedAlbumsReplacePeer` replaces one direction wholesale, so a peer withdrawing everything cannot clear our own offers, and the route never answers a peer with the index we hold about it. `refreshPeerIndexes` pulls every linked peer's index on the invite loop and the moment a peer says it changed (`/index/nudge`); a panel's match read refreshes it too, bounded by `INDEX_REFRESH_DEADLINE_MS`, so the answer is current without a person waiting out a peer's dial. `refreshPeerAlbums` hints the open panels (`emitPanelEvent('index')`) only when `indexChanged` says the replacement differed — a panel's own read comes back through it, so an ungated hint would re-hint the page that just asked. `myMatches` offers on every visit to a person's panel (`offerAlbumsTo`) — including when they now own nothing, because an empty offer is how a peer learns their albums are gone — and that visit is the only moment their credential is in hand; `POST /me/albums/publish` is the same offer as a route. Reading from Immich is the point: ownership is answered by the server that owns the albums, so nothing a client posts can add an album the caller does not own or omit one they do. The sidecar holds no credential for a human and `GET /albums` is scoped to one, which is why this rides the caller's session rather than a stored key, and why a request body names only the peer. |
| `index-offer.ts`       | **When a person's albums are worth offering again, pure.** `opensSession` (the first request after `SESSION_QUIET_MS` of quiet), `offerWindowMs` (60s, doubling per refresh that found nothing, capped at `OFFER_MAX_MS`) and `indexChanged` (a name, count, date span or owner moved — order is not a change). The window answers to evidence: a change snaps it back to the base so work is followed closely, silence stretches it so an untouched library costs four reads an hour, because the cost is the PAYLOAD — one `GET /albums` whose size grows with the library (~620 bytes an album, measured).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `index-freshness.ts`   | **Offering a person's albums from the traffic they already make.** `noteIndexTraffic` runs on the way past the Immich proxy — the sidecar IS the front door, so the web UI and the phone app both announce a session — and reads their albums as them, coalesced to one read per credential at a time and never in a request's path. A refresh only READS unless something changed: `offerToEveryPeer` compares before it writes, so an unchanged list costs no store write and no peer dial, and a changed one nudges (`nudgePeerIndex`). `offerAdminAlbums` does the same with the configured key, which is what makes a link useful the moment it is made.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `album-invite.ts`      | **Starting a reunion from the panel.** `invitePeerToReunite` shares the caller's OWN album with the one person on the peer who owns the other half: the album is re-derived from the caller's Immich list and the person from the index the peer itself published, so a request can name either but establish neither. `ensureContributor` is called with `invitation: true`, which skips `addedRecord` — the membership is left outside the attribution ledger so it reads as a human's, and `detectInvitesOnce` finds it as an invitation on the next poll (`ISA_SYNC_POLL_MS`), recording the owner mapping. A failed add is read back off the album and reported rather than trusted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `album-suppression.ts` | **Refusing a second stub for a photo the album already holds.** `mappingsSharingAlbum` names the mappings whose local half is one album, and `existingCopyInAlbum` finds a ledger row another of them already materialised. `materialiseRef` consults it before uploading and, finding one, records the row against THIS mapping instead of making a second stub — Immich cannot collapse them, because the bytes differ by a random tail, so a 3-way mesh would show the photo twice. Scoped to the album: the same photo in two different albums is the ordinary case.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `audit.ts`             | **The trail, in the album's own comments.** `auditLine(mappingId, albumId, event, text)` posts one line as this household's bot and records it twice in `seen_activity`: `audit:<event>:<albumId>` so a retrying loop cannot write it again, and `local:<activityId>` so `syncCommentsOnce` does not push it back to the peer as though a person had written it. Four events, each written by the one request that can write it: `invited` (the panel's Invite), `reunited` (adoption — `unifyOwnAlbum` and `ensureMirror`'s adopt branch), `accepted` (the origin, when `handleReunified` hears the peer merged it) and `unreunited` (`/me/unreunite`, between purging the peer's stubs and `stripAlbumBots` taking the bot's membership away). Called where the owner's credential is in hand — adoption — because the bot must be a MEMBER to comment and only an album's owner can grant that.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `peer-mapping-id.ts`   | **The id a peer addresses an album by.** `peerAlbumMappingId` answers one question the two push paths (the watcher's refs, `comments.ts`'s activity) both ask: a mirror WE hold is addressed by the id the origin minted for it (`remoteMappingId`, or `remoteAlbumId` for a protocol-2 peer), while our OWN album is addressed by our album id — naming it with one of the mirror ids a mapping also carries would write to a mapping that is not ours. Empty when a member mirror has neither remote id, so a caller refuses rather than addresses an album nobody offered.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `album-grant.ts`       | **The membership a reunified album's writers need.** `peerContributors` reads the distinct contributors from the peer's manifest; `grantAlbumWriters` adds each one's `person-<id>` account to the adopted album as an EDITOR, through `ensureContributor` on the album OWNER's forwarded credentials. Only an owner can add a member, and the request that reunifies is the only place that credential exists — so the grant happens at adoption, never lazily in a reconcile. The manifest pull is bounded by `GRANT_MANIFEST_DEADLINE_MS`: a peer that cannot answer in seconds grants nothing rather than holding up the owner's request.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `comments.ts`          | Cross-server comments. The origin album is the source of truth: members pull the canonical list and push their own, gated by a cheap activity-count statistic so messages land in seconds without heavy polling. Includes the inbound `handleActivity`/`handleComments` handlers. `startCommentLoop` runs the fast lane.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

**Why loops and not just webhooks:** nudges make the common case instant, but the timed
sweep is the safety net — a lost nudge costs nothing because the next scheduled handshake
catches everything (fail-open by design). `ISA_RECONCILE_DEBUG=1` traces every decision.

## Holding the loops (rig-only)

`sweeps.ts` holds what the background loops are doing. `setSweepsPaused` — driven by
`POST /test/pause-sweeps` under `ISA_TEST_HOOKS` — is read by the three loop schedulers:
`startWatchLoop` (`watchOnce` + `reconcileOnce`), `startInviteLoop`, `startCommentLoop`. A held loop
returns before its tick counter, so `ticks` in `/sync/status` stays put and "the loop did not look"
is observable.

The same module owns the per-loop slots (`startSweep`/`finishSweep`), which are the overlap guards
those loops used to keep privately, and `sweepsAreIdle`/`whenSweepsIdle` read them. Holding answers
`{ paused, idle }` and waits — bounded by `SWEEP_DRAIN_MS` — for a cycle already in flight to finish
first: a pause acknowledged while a watch cycle was still running would let a lane claim "no sweep
delivered this" while one still could.

Nudge handlers are NOT gated: `handleIndexNudge`, `handleInvitationsNudge` and the panel routes run
their own pulls and pushes whatever the loops are doing. That is the whole point — a lane that holds
every loop and still sees an invitation accepted on an open page, a row move and both albums complete
has proved the wire did it, with no cadence left to argue about. Release it in the same run
(`{ "paused": false }`); a rig is recreated per lane, so a forgotten hold cannot leak past one.

## Why sharing is per person, and links are not shareable objects

Sharing names a **person**, never a whole server. A server link is not a person, so it gets no
marker user and never appears in Immich's people picker; it is created by redeeming a share link
and destroyed from the panel (`…/unlink`). This also means `ISA_PUBLISH_USER_DIRECTORY=false` disables
native invitations with that peer — with no directory there is nobody to name — and share links
remain the way in.

An invitation may name several people. `Mapping.forPeerUserIds` holds the whole set, and the
member side follows it in both directions: people added upstream are added to the mirror, and a
person dropped while others remain is removed from it. Without that second half a revocation for
one person would appear to work and silently do nothing.

## Why invitations are detected as the marker, not with the admin key

`GET /albums` is scoped per user, so the admin key only ever sees the admin's own albums —
which is exactly why a non-admin cannot share cross-server through a share link today. Asking
_as the stand-in_ sidesteps it: it does not matter who owns the album, only that the stand-in
was invited to it.

Three Immich behaviours this has to allow for:

- the album **owner** appears inside `albumUsers` with `role: 'owner'`, and `GET /albums`
  returns no `ownerId` at all — so the owner is only discoverable there;
- a stand-in that _owns_ an album is a mirror we created for inbound content, not an
  invitation, so those are skipped;
- adding a user who is already the owner returns **200 and is silently ignored**, so a 200 is
  never proof an invitation took. Read `albumUsers` back.

## Provenance is load-bearing

`Mapping.via` records whether a share came from a link or an invitation, and two rules depend
on it:

1. **Only `via: 'invite'` mappings may be retired** when a stand-in vanishes from an album. A
   link-redeemed mapping never had a stand-in added to its album, so it is absent from that
   list by design — retiring those here would silently unshare every link-based album.
2. **Only `via: 'invite'` mappings are offered** on the `/invitations` peer route. Offering link ones
   would re-mirror albums the member already handled through `join`, and worse, would silently
   undo `leaveAlbum` on the next poll, because leaving removes the member's mapping but not the
   origin's.

Invitations are **pulled**, never pushed: a member with no inbound reachability still syncs
perfectly well by pulling, so a push-based invitation would fail for exactly the households
that most need this.

## Only an album's owner can grant a membership

Immich scopes album membership writes to the album's owner. The household admin key answers
`403 albumUser.create` on an album a different person owns, and so does the house bot (a viewer), so
there is no key the sidecar holds that can widen someone else's album. Three cases follow, and the
code depends on which one it is in:

- **An adopted album** (`Mapping.adopted`) belongs to a local human. Its stub-writing accounts are
  therefore granted exactly once, at adoption, on that owner's forwarded credential —
  `grantAlbumWriters` and `grantInvitedHumans` in `album-grant.ts`. A reconcile that runs later has
  no owner credential to offer, so a contributor granted nothing fails every cycle instead of being
  repaired in place, and a contributor the peer only starts offering later needs the owner in the
  loop again. `syncMirrorMembers` answers an adopted mapping before it reads anything — a local
  human owns that album, so no key the sidecar holds can widen it — and logs one line per mapping
  naming the peer whose change needs the reunion re-run, rather than looping the 403.
- **Un-reunifying** runs on the owner's credential too, so it takes those accounts back off
  (`stripAlbumBots`), after the peer's stubs are purged. A membership left behind keeps the sidecar's
  read access to a private album and makes that album look like a live mirror to anything
  enumerating albums by stand-in key — the same mistake in reverse.
- **One album, two shares** is what album-level suppression is for (`album-suppression.ts`): the photo must be
  materialised once and carried by two ledger rows, so withdrawing one share's copy leaves the other's claim
  standing. Immich cannot dedupe the stubs itself — they differ by a random tail, so that each is a distinct asset.
- **A mirror the sidecar creates** is owned by the sidecar's own key, so it adds members directly.
- **An album a local human owns and invited a peer into** cannot gain a bot at all without that
  human acting; no credential for it is ever in hand.

`Mapping.hostSlug` names the account that READS an album — the house bot on an adopted album, the
owning stand-in on a mirror. It is not the account that may widen one, and using it for a membership
write is what a silent 403-per-cycle loop looks like.
